<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_CARLITO"
        app:queryPatterns="@array/ask_carlito_query_patterns">
        <intent
            android:action="ai.carlito.app.action.ASK_CARLITO"
            android:targetPackage="ai.carlito.app"
            android:targetClass="ai.carlito.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
