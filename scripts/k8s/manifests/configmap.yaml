apiVersion: v1
kind: ConfigMap
metadata:
  name: carlito-config
  labels:
    app: carlito
data:
  carlito.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.carlito/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Carlito Assistant",
            "workspace": "~/.carlito/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Carlito Assistant

    You are a helpful AI assistant running in Kubernetes.
