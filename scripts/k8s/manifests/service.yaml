apiVersion: v1
kind: Service
metadata:
  name: carlito
  labels:
    app: carlito
spec:
  type: ClusterIP
  selector:
    app: carlito
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
