---
summary: "CLI reference for `carlito clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `carlito clawbot ...`
  - You need migration guidance to current commands
title: "Clawbot"
---

# `carlito clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `carlito clawbot qr` (same behavior as [`carlito qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `carlito clawbot qr` -> `carlito qr`
